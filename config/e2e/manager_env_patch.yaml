apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-dns-operator-controller-manager
  namespace: gateway-dns-operator-system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: ADGUARD_URL
          value: http://adguard-home.example/control
